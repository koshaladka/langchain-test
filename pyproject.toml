[project]
name = "langchain2"
version = "0.1.0"
description = "AI-диспетчер входящих задач команды (LangGraph)"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.4",
    "langchain-openai>=1.3.0",
    "langfuse>=4.7.1",
    "langgraph>=1.2.4",
    "load-dotenv>=0.1.0",
]
